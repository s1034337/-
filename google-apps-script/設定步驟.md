# 老師共用排行榜設定

1. 新增一份 Google 試算表，名稱可設為「日日讀形音義排行榜」。
2. 在試算表上方選擇「擴充功能」→「Apps Script」。
3. 刪除編輯器原本的內容，貼上 `Code.gs` 的全部程式，然後按「儲存」。
4. 選擇右上角「部署」→「新增部署作業」。
5. 類型選「網頁應用程式」，執行身分選「我」，誰可以存取選「任何人」。
6. 按「部署」並完成 Google 授權。
7. 複製結尾為 `/exec` 的「網頁應用程式網址」，貼回 Codex 對話。

收到網址後，網站就能把每位學生送出的排行榜成績寫入這份試算表。
